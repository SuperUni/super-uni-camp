spring:
  application:
    name: uni-common
  profiles:
    active: native
  cloud:
    config:

server:
  port: 8903

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8902/eureka/