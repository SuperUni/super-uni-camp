spring:
  application:
    name: uni-config
  profiles:
    active: native
  cloud:
    config:

server:
  port: 8901

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8902/eureka/